"use client";

import { MyPlateDashboard } from "@/components/bidding/MyPlateDashboard";

/** Role dashboard — GET /bids/my-plate. Not the Estimates list. */
export default function RoleDashboardPage() {
  return <MyPlateDashboard />;
}
