# Base URL of the NestJS backend (no trailing slash).
# Frontend runs on port 3002; backend runs on port 3000.
NEXT_PUBLIC_API_BASE_URL=http://172.20.20.225:3000

# Port for the Next.js dev server (so it doesn't conflict with backend on 3000).
PORT=3002
